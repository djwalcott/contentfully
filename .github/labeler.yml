# Automatically adds labels to created pull requests
# Pull request labels are then used to create automatic release notes
# Docs: https://github.com/actions/labeler

assets:
  - 'src/assets/**/*'

store:
  - 'src/storage/**/*'

automation:
  - '.github/'

ignore-for-release:
  - all: ['**/*.md', '**/*.mdx']
